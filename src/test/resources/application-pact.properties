# IDAM
auth.idam.client.baseUrl=http://localhost:${wiremock.server.port}/idam

# S2S
# This property *must* retain its name because it is required by the ServiceTokenParserConfiguration class in the
# auth-checker-lib library, on which there is a dependency
auth.provider.service.client.baseUrl=http://localhost:${wiremock.server.port}/s2s

#ttl for the latest version objects cached
definition.cache.latest-version-ttl.secs=3
# PACT Variables TODO SET THE VARIABLES IN THE JENKINS FILES
pact.broker.scheme=${PACT_BROKER_SCHEME:http}
pact.broker.baseUrl=${PACT_BROKER_URL:localhost}
pact.broker.port=${PACT_BROKER_PORT:80}
pact.provider.version=${PACT_BROKER_VERSION:1.0.0}
pact.broker.consumer.tag=master
server.port=4452
